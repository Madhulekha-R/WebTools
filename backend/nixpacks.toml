[phases.setup]
nixPkgs = ["nodejs-18_x", "libreoffice", "chromium"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["echo 'No build step required for backend'"]

[start]
cmd = "npm start"
